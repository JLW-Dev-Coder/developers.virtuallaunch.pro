{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'echo \"$CLAUDE_TOOL_INPUT\" | grep -qE \"wrangler (deploy|publish)\" && echo \"[hook] REMINDER: Run build before deploying\" || true'"
          }
        ]
      }
    ]
  }
}
